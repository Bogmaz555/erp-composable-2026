global:
  namespace: erp-dev

apiGateway:
  replicaCount: 1
  authEnforce: false
  useKeycloakJwks: false
  pilot: "0"

analyticsService:
  replicaCount: 1

resources:
  requests:
    cpu: 50m
    memory: 128Mi
