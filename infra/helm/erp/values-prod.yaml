global:
  namespace: erp-prod

apiGateway:
  replicaCount: 3
  authEnforce: true
  useKeycloakJwks: true
  pilot: "1"

analyticsService:
  replicaCount: 2

resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi
