apiVersion: apps/v1
kind: Deployment
metadata:
  name: plm-service
  namespace: erp-apps
  labels:
    app: plm-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plm-service
  template:
    metadata:
      labels:
        app: plm-service
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "erp-apps"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/erp/plm-service"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/erp/plm-service" -}}
          {{ range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{ end }}
          {{- end -}}
    spec:
      serviceAccountName: erp-apps-sa
      containers:
      - name: plm-service
        image: plm-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3009
        env:
        - name: PORT
          value: "3009"
        command: ["/bin/sh", "-c"]
        args:
        - |
          source /vault/secrets/config || true
          npm run start
        livenessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: plm-service
  namespace: erp-apps
spec:
  selector:
    app: plm-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3009
