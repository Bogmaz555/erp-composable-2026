apiVersion: apps/v1
kind: Deployment
metadata:
  name: mes-service
  namespace: erp-apps
  labels:
    app: mes-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mes-service
  template:
    metadata:
      labels:
        app: mes-service
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "erp-apps"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/erp/mes-service"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/erp/mes-service" -}}
          {{ range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{ end }}
          {{- end -}}
    spec:
      serviceAccountName: erp-apps-sa
      containers:
      - name: mes-service
        image: mes-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3003
        env:
        - name: PORT
          value: "3003"
        command: ["/bin/sh", "-c"]
        args:
        - |
          source /vault/secrets/config || true
          npm run start
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: mes-service
  namespace: erp-apps
spec:
  selector:
    app: mes-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3003
