apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance
  namespace: erp-apps
  labels:
    app: finance
spec:
  replicas: 1
  selector:
    matchLabels:
      app: finance
  template:
    metadata:
      labels:
        app: finance
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "erp-apps"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/erp/finance"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/erp/finance" -}}
          {{ range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{ end }}
          {{- end -}}
    spec:
      serviceAccountName: erp-apps-sa
      containers:
      - name: finance
        image: finance:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3006
        env:
        - name: PORT
          value: "3006"
        command: ["/bin/sh", "-c"]
        args:
        - |
          source /vault/secrets/config || true
          npm run start
        livenessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3006
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: finance
  namespace: erp-apps
spec:
  selector:
    app: finance
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3006
