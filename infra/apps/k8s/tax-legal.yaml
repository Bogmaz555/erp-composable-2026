apiVersion: apps/v1
kind: Deployment
metadata:
  name: tax-legal
  namespace: erp-apps
  labels:
    app: tax-legal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tax-legal
  template:
    metadata:
      labels:
        app: tax-legal
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "erp-apps"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/erp/tax-legal"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/erp/tax-legal" -}}
          {{ range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{ end }}
          {{- end -}}
    spec:
      serviceAccountName: erp-apps-sa
      containers:
      - name: tax-legal
        image: tax-legal:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3010
        env:
        - name: PORT
          value: "3010"
        command: ["/bin/sh", "-c"]
        args:
        - |
          source /vault/secrets/config || true
          npm run start
        livenessProbe:
          httpGet:
            path: /health
            port: 3010
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3010
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: tax-legal
  namespace: erp-apps
spec:
  selector:
    app: tax-legal
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3010
