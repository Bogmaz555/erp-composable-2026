apiVersion: apps/v1
kind: Deployment
metadata:
  name: proc-service
  namespace: erp-apps
  labels:
    app: proc-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: proc-service
  template:
    metadata:
      labels:
        app: proc-service
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "erp-apps"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/erp/proc-service"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/erp/proc-service" -}}
          {{ range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{ end }}
          {{- end -}}
    spec:
      serviceAccountName: erp-apps-sa
      containers:
      - name: proc-service
        image: proc-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3005
        env:
        - name: PORT
          value: "3005"
        command: ["/bin/sh", "-c"]
        args:
        - |
          source /vault/secrets/config || true
          npm run start
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: proc-service
  namespace: erp-apps
spec:
  selector:
    app: proc-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3005
