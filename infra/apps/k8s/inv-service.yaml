apiVersion: apps/v1
kind: Deployment
metadata:
  name: inv-service
  namespace: erp-apps
  labels:
    app: inv-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inv-service
  template:
    metadata:
      labels:
        app: inv-service
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "erp-apps"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/erp/inv-service"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/erp/inv-service" -}}
          {{ range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{ end }}
          {{- end -}}
    spec:
      serviceAccountName: erp-apps-sa
      containers:
      - name: inv-service
        image: inv-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3004
        env:
        - name: PORT
          value: "3004"
        command: ["/bin/sh", "-c"]
        args:
        - |
          source /vault/secrets/config || true
          npm run start
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: inv-service
  namespace: erp-apps
spec:
  selector:
    app: inv-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3004
