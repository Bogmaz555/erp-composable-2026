apiVersion: apps/v1
kind: Deployment
metadata:
  name: pm-service
  namespace: erp-apps
  labels:
    app: pm-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pm-service
  template:
    metadata:
      labels:
        app: pm-service
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "erp-apps"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/erp/pm-service"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/erp/pm-service" -}}
          {{ range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{ end }}
          {{- end -}}
    spec:
      serviceAccountName: erp-apps-sa
      containers:
      - name: pm-service
        image: pm-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3002
        env:
        - name: PORT
          value: "3002"
        command: ["/bin/sh", "-c"]
        args:
        - |
          source /vault/secrets/config || true
          npm run start
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: pm-service
  namespace: erp-apps
spec:
  selector:
    app: pm-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3002
