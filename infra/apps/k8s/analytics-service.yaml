apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: erp-apps
  labels:
    app: analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "erp-apps"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/erp/analytics-service"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/erp/analytics-service" -}}
          {{ range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{ end }}
          {{- end -}}
    spec:
      serviceAccountName: erp-apps-sa
      containers:
      - name: analytics-service
        image: analytics-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3012
        env:
        - name: PORT
          value: "3012"
        command: ["/bin/sh", "-c"]
        args:
        - |
          source /vault/secrets/config || true
          npm run start
        livenessProbe:
          httpGet:
            path: /health
            port: 3012
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3012
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: erp-apps
spec:
  selector:
    app: analytics-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3012
