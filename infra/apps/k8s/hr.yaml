apiVersion: apps/v1
kind: Deployment
metadata:
  name: hr
  namespace: erp-apps
  labels:
    app: hr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hr
  template:
    metadata:
      labels:
        app: hr
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "erp-apps"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/erp/hr"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/erp/hr" -}}
          {{ range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{ end }}
          {{- end -}}
    spec:
      serviceAccountName: erp-apps-sa
      containers:
      - name: hr
        image: hr:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3011
        env:
        - name: PORT
          value: "3011"
        command: ["/bin/sh", "-c"]
        args:
        - |
          source /vault/secrets/config || true
          npm run start
        livenessProbe:
          httpGet:
            path: /health
            port: 3011
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3011
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: hr
  namespace: erp-apps
spec:
  selector:
    app: hr
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3011
